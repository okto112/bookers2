<h1>編集画面</h1>

<%= form_with model: @book do |f| %>

  <h4>本のタイトル</h4>
  <%= f.text_field :title %>

  <h4>感想</h4>
  <%= f.text_area :body %>

  <h4>投稿ユーザー画像</h4>
  <%= f.file_field :image, placeholder: "投稿画像", accept: "image/*" %>

  <%= f.submit '保存' %>

<% end %>